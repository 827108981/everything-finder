# 配置说明

配置文件默认位置：

```text
%LOCALAPPDATA%\LocalFullTextSearch\config\settings.json
```

主要配置项：

- `page_size`：每页搜索结果数量。
- `max_results`：单次搜索最多展示的结果数量；完整命中数量仍会统计。
- `auto_search`：是否在停止输入后自动搜索。
- `search_debounce_ms`：自动搜索等待时间。
- `parser_workers`：普通文档解析线程数，影响 TXT、Office OpenXML、文本型 PDF 等首次扫描速度。
- `slow_file_workers`：ZIP、老版 Office 等慢任务线程数。
- `process_parser_workers`：大型 XLSX/DOCX 解析进程数，默认 2。进程间不共享 Python GIL，但每个进程都会占用独立内存。
- `index_write_batch_size`：批量写入数据库的文件数，默认 32。
- `normal_pending_tasks`：普通解析通道最多同时提交的任务数，默认 96。
- `ocr_pending_tasks`：图片 OCR 通道最多同时提交的任务数，默认 2。
- `slow_pending_tasks`：ZIP/老版 Office 通道最多同时提交的任务数，默认 2。
- `process_pending_tasks`：大型 Office 进程通道最多同时提交的任务数，默认 2；建议不要明显高于进程数。
- `large_office_process_min_bytes`：DOCX/XLSX/XLSM 进入进程池的最小文件大小，默认 4 MB。
- `process_max_tasks_per_child`：每个 Office 解析子进程处理多少个文件后回收，默认 8，用于限制长期内存累积。
- `max_pending_parse_tasks`：旧版全局队列配置，仅为兼容旧配置保留，新调度器不再使用全局上限。
- `enable_ocr`：是否启用 OCR，团队版默认 `true`。
- `ocr_images`：是否索引图片 OCR，默认 `true`。
- `ocr_scanned_pdf`：是否对疑似扫描 PDF 页面执行 OCR，默认 `true`。
- `ocr_workers`：图片 OCR 任务线程数，建议 1～2。
- `ocr_min_confidence`：默认隐藏低于该置信度的 OCR 结果，可在高级筛选中临时显示。
- `min_ocr_image_pixels`：低于该像素数的小图片跳过 OCR，仅索引文件名和路径。
- `max_ocr_image_side`：OCR 首轮文字检测边长，默认 960。检测框会映射回原图识别；首轮质量不足时，程序会对原图进行重叠分块复核。
- `max_zip_file_count`：单个 ZIP 内最多解析的文件数量。
- `max_zip_uncompressed_bytes`：单个 ZIP 解压后允许的最大体积。
- `max_zip_depth`：ZIP 嵌套解析深度。
- `monitor_file_changes`：是否监控文件变化；短时间内的事件会合并，并提示用户手动更新索引，不会自动启动全量扫描。
- `retry_failed_files`：增量扫描时是否重新尝试失败文件。
- `compute_full_hash`：是否使用完整 SHA-256 辅助判断文件变化；更可靠但会降低扫描速度。
- `excluded_dirs`：扫描时排除的目录名。
- `excluded_file_patterns`：扫描时排除的临时文件通配符。

修改配置后重启程序或在设置窗口保存即可生效。

大型 Office 子进程将解析结果写入 `%LOCALAPPDATA%\LocalFullTextSearch\temp\process_results\<扫描任务>`。主进程写库后立即删除结果文件，扫描结束或取消时清理整个任务目录。
