# 本地多格式全文搜索工具 - 使用说明

适用版本：1.3.7（2026 年 8 月 3 日）

## 一、首次使用

1. 解压完整 ZIP，双击“本地多格式全文搜索工具.exe”。不要只复制 EXE，必须保留同目录的 `_internal`。
2. 进入“索引管理”，点击“添加搜索范围”，选择一个或多个需要检索的本地文件夹。
3. 点击“更新全部”建立索引。索引期间可以安全暂停、切换模式、继续或取消。
4. 全部可解析文件完成后，搜索自动开放。视频和仅元数据完成项不会阻塞搜索。
5. 如果仍有阻断项，进入“未成功索引”按第五节处理。

## 二、搜索和查看结果

1. 在搜索框输入关键词。中文查询会自动兼容空格差异，例如“拔掉 3 个传感器”可以命中“拔掉3 个传感器”。
2. 根据需要选择“完全包含”“完整短语”“全部关键词”“任意关键词”或“正则表达式”。
3. 可按文件格式、搜索范围及高级条件筛选。
4. 命中的关键词会在中间结果和右侧预览中同时黄显。
5. 结果卡片会显示命中位置、上下文、文件路径、修改时间和命中次数；一个文件有多处命中时可以展开全部。
6. 双击结果或点击右侧“打开文件”打开原文件，也可以打开所在文件夹或复制路径。
7. 普通中文或英文名称、路径和正文查询优先使用本地 FTS 倒排索引，不重新读取源文件；文件名结果会先显示，正文结果随后合并。
8. 搜索较慢时会显示当前阶段、已找到文件数和耗时；正则、忽略空格或忽略连字符等扫描型搜索还会显示已检查数量和真实百分比。点击“停止搜索”可及时中断并保留已显示的部分结果。

## 三、索引格式

- PDF：提取原生文字；疑似扫描页会执行 OCR。
- Word：支持 `.docx`；`.doc` 通过本机 Microsoft Office、WPS Office 或 LibreOffice 转换后解析。
- Excel：支持 `.xlsx`、`.xlsm`；`.xls` 通过本机转换器解析。
- PowerPoint：支持 `.pptx`；`.ppt` 通过本机转换器解析。
- 文本：支持 `.txt`、`.log`、`.csv`、`.md`、`.json`、`.xml`、`.ini`。
- 图片：支持 `.jpg`、`.jpeg`、`.png`、`.bmp`、`.tif`、`.tiff`，默认执行 OCR。
- ZIP：在安全限制内递归解析压缩包中的受支持文件，并恢复常见旧式中文文件名编码。
- 视频：不解析视频正文，也不计入完整索引失败数量。

## 四、索引速度和性能模式

- 普通模式适合边索引边办公，资源占用更克制。
- 性能模式适合电脑空闲时首次全量索引。程序根据 CPU、可用内存和磁盘情况动态提高 PDF、OCR 和普通文件车道并发，允许更积极地利用空闲资源。
- 切换模式前先点击“暂停”，等待界面明确显示“已暂停”，切换后再点击“继续”。
- 文件没有变化时会直接跳过；重复内容可以复用解析结果。大型扫描 PDF、图片 OCR 和复杂压缩包仍可能耗时较长。
- 预计时间只是动态参考，当前阶段、完成数和是否持续前进更重要。

## 五、未成功索引与搜索保底

先确认索引任务已经停止，再进入“未成功索引”。建议按以下顺序处理：

1. 点击“重新尝试”，让程序按当前文件和环境再处理一次。
2. 对确认损坏、加密、缺少转换组件或超过安全限制的文件，勾选后点击“从当前索引范围排除”。确认后程序在后台更新排除审计和全文索引，窗口仍可移动、切换或关闭；页面会显示当前阶段、文件数进度以及是否正在执行大型 FTS 操作。确认排除最后一个阻断项后，程序会自动发布当前索引并开放搜索，不需要再次更新。
3. 如果仍有个别任务无法正常收敛，点击顶部操作栏常显的“强力完成本次索引”。存在阻断文件时，第一次确认会执行最后一次正常恢复重试；仍失败后第二次确认才会排除剩余阻断项。文件已经全部处理但仍有残留任务、未发布候选或待发布全文索引时，该按钮也会亮起，并直接修复这些残留状态、发布索引后开放搜索。

“强力完成本次索引”的状态：

- 灰色禁用：索引已经真正就绪，或索引/后台更新任务仍在运行。
- 蓝色可用：存在阻断项；或者阻断项为 0，但仍有残留解析任务、未发布候选、未收敛批次或待发布全文索引。

顶部状态显示“文件已完成，但索引状态待修复”时，不要重新建立全部索引，直接进入“未成功索引”点击“强力完成本次索引”。修复在原数据库事务中完成，失败或取消会回滚，不使用数据库副本。

排除操作不会删除或修改原文件，但被排除文件的文件名、路径和正文都不参与搜索。可在“已人工排除”页恢复并重试；原文件发生变化后，旧排除记录会自动失效。

后台更新期间，搜索、新索引任务、恢复纳入和强力完成会暂时禁用，防止出现混合结果。点击“取消索引更新”或在更新期间关闭程序，会先中断任务并回滚本次事务；不会提交半完成的全文索引。

## 六、常见失败原因

- OCR 不可用或模型缺失：确认使用完整打包目录，不要只复制 EXE。
- 老版 Office 转换失败：安装 Microsoft Office、WPS Office 或 LibreOffice 后重新更新。
- 文件损坏或加密：使用原软件确认文件能否打开，并解除密码保护。
- ZIP 内部文件失败：检查压缩包是否损坏、加密或超过成员数量、解压总量、嵌套层级等安全限制。
- 无进展超时：程序会回收卡住的隔离任务并重试；同一安全游标重复卡住后才进入“未成功索引”。

## 七、亲自验证保底功能

发行目录根部提供“异常文件保底功能演示.bat”：

1. 先关闭正在运行的正式程序，再双击该批处理文件。
2. 演示会重置包内独立的 `failure-fallback-demo-data`，模拟损坏 PDF、密码 Word、失败图片和超限 ZIP 等阻断项。
3. 在“未成功索引”中亲自尝试“重新尝试”“从当前索引范围排除”和“强力完成本次索引”。
4. 完成后进入搜索页，搜索 `DEMO_SEARCH_OK`，应命中正常演示文件。

演示入口不读取或修改正式数据库 `%LOCALAPPDATA%\LocalFullTextSearch\data\search_index.db`。再次启动批处理会重置演示数据。

## 八、升级版本与数据库

1. 退出旧版程序。
2. 把新版 ZIP 解压到一个完整的新目录，不要把新 EXE 单独覆盖到旧 `_internal` 中。
3. 运行新版 EXE。索引数据库默认仍位于 `%LOCALAPPDATA%\LocalFullTextSearch\data\search_index.db`，新版会自动复用并按需迁移。
4. 安装新解析规则后可执行一次“更新全部”；未变化且仍有效的内容会尽量复用，不要求先删除数据库。

只有明确确认数据库损坏或需要彻底重建时才删除数据目录。回滚旧版本前应先退出程序，并同时保留当前数据库和日志；不要混用不同版本的 `_internal`。

## 九、程序点了没反应时需要提供的日志

优先复制整个目录：

`%LOCALAPPDATA%\LocalFullTextSearch\logs`

重点文件包括 `startup.log`、`startup-state.json` 和 `app.log`（以及轮转出的 `app.log.*`）。如果本地数据目录不可写，再检查：

`%TEMP%\LocalFullTextSearch`

如果在 Windows 安全提示、EDR 或 DLL 加载阶段就被系统拦截，程序可能来不及生成日志。此时同时提供完整发行目录结构、Windows 提示截图和安全软件拦截记录。

## 十、隐私和数据位置

工具只在本机读取、解析和索引文件，不上传文件内容或搜索记录。

索引数据库、设置、日志和 OCR 缓存默认保存在：

`%LOCALAPPDATA%\LocalFullTextSearch`

删除该目录会清空本机索引和缓存；不会删除原始文档。
