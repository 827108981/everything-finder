# 使用说明

1. 启动程序后点击“添加目录”，选择一个或多个需要搜索的本地文件夹。
2. 点击“开始索引”。索引过程在后台线程执行，可暂停、继续或取消。
3. 索引完成后，在搜索框输入关键词，选择搜索模式并点击“搜索”。
4. 结果按卡片分页展示，包含文件名、命中位置、上下文、命中次数、来源类型、修改时间和路径。
5. 选中结果后，右侧预览区会显示文件信息、命中上下文和图片预览。
6. 双击结果可打开文件；也可点击“打开文件”或“打开所在文件夹”。
7. 点击“失败文件”可查看无法解析、仅元数据、转换器缺失或 OCR 不可用的文件及原因，并可导出明细或汇总。

## 搜索模式

- 精确包含：搜索词必须作为连续字符串出现。
- 全部关键词：输入的多个关键词必须全部出现，顺序不限。
- 任一关键词：多个关键词出现任意一个即可。
- 完整短语：适合带引号的完整句子或术语。
- 仅文件名：只搜索文件名和路径，不搜索正文。

## 隐私说明

本工具仅在本机处理和索引文件，不上传文件内容或搜索记录。数据库中会保存提取出的文本和文件元数据，用户可删除 `%LOCALAPPDATA%\LocalFullTextSearch` 清空索引与缓存。

## 格式说明

- 图片和扫描 PDF 默认执行 OCR。
- `.doc`、`.xls`、`.ppt` 需要本机 Microsoft Office 或 LibreOffice 转换器；未安装时会显示为“converter_missing”，不会计为程序崩溃。
- `.zip` 会在安全限制内解析内部受支持文件。
- `.mp4` 默认只索引文件名和路径。
