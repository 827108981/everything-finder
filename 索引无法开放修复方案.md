# 索引无法开放与搜索卡慢修复方案

## 一、现场结论

现场不是人工排除失败，而是“文件完成状态、解析任务状态、候选全文索引状态和界面状态”没有统一收敛。

2026-08-03 日志证据：

```text
11:30:44  开始人工排除 41 个文件
11:42:22  排除完成：excluded_files=41
           blocking_files=0
           ready=false
           triggered_full_fts_rebuild=false

12:31:04  Candidate FTS remains unpublished
           failed gates=incomplete_files,unfinished_tasks

13:11:07  Candidate FTS remains unpublished
           failed gates=incomplete_files,unfinished_tasks
```

因此现场显示：

```text
当前范围 7387/7387
待处理 0
阻断项 0
已人工排除 41
搜索仍未开放
强力完成按钮灰色
```

这些信息分别使用了不同判断口径。顶部完成数只统计文件状态；“强力完成”只检查失败文件列表；真正的搜索开放还检查 root、批次和 FTS 状态。

索引管理页显示的“索引已就绪”目前只是“没有索引任务正在运行”时的固定文案，不代表 `database.index_readiness().ready=true`，必须修正。

## 二、当前电脑的处理原则

1. 不删除现有 `search_index.db`，避免丢失已经完成的正文索引。
2. 修复前不要反复点击“更新全部”或“性能模式更新”。现场日志证明重复更新仍会被相同发布门禁拒绝。
3. 升级修复版前关闭程序，并备份：

   ```text
   C:\Users\50259127\AppData\Local\LocalFullTextSearch\data\search_index.db
   ```

4. 修复版必须直接打开并修复原路径数据库，不要求用户删除数据库或重新执行全量索引。
5. 修复版启动后显示具体未就绪原因；用户点击“强力完成本次索引”后，程序修复残留状态、重建并发布 FTS。只有事务全部成功才开放搜索。

不建议用户手工执行 SQL。状态修复必须由程序在事务中完成并保留日志。

## 三、统一索引就绪诊断

### 3.1 单一数据源

扩展 `database.index_readiness()`，或新增其内部使用的 `readiness_diagnostics()`。所有页面和按钮必须使用同一份结果，不再各自推算。

至少返回：

```text
blocking_files
unfinished_tasks
active_runs
content_fts_dirty
full_batch_incomplete
unready_roots
unpublished_candidates
enabled_roots
ready
not_ready_reasons
repairable
```

`not_ready_reasons` 使用稳定原因码，例如：

```text
blocking_files
unfinished_tasks
active_run
content_fts_dirty
full_batch_incomplete
unready_root
unpublished_candidate
integrity_error
```

### 3.2 统一任务终态

定义共享的任务终态集合，并由人工排除、候选发布、强力完成和统计代码共同使用：

```text
complete
failed
superseded
invalidated
cancelled
```

当前人工排除校验已经把 `failed`、`superseded` 视为终态，但候选发布门禁只认可 `complete`、`invalidated`、`cancelled`，这是现场 `unfinished_tasks` 的一个直接来源。

失败任务是否阻止开放，应由对应的可见文件状态决定：

- 文件仍在当前范围且失败：由 `blocking_files` 阻止；
- 文件已经人工排除：不阻止；
- 文件已成功但残留旧任务：旧任务改为 `invalidated`；
- 真正处于 `queued/running/spooled/paused` 等活动状态的任务：由 `unfinished_tasks` 阻止。

## 四、人工排除后的自动收敛

`exclude_files_from_index()` 在同一个事务中完成以下步骤：

1. 校验选中文件和活动任务。
2. 写入人工排除审计。
3. 将被排除文件对应的非终态任务改为 `cancelled`，清理 lease；历史终态记录保留。
4. 清除被排除文件的 `files_fts` 和不可见 `content_fts` 行。
5. 使用统一诊断重新检查当前可见范围。
6. 如果可见阻断文件为 0、可见活动任务为 0，则重建或完成候选 FTS 发布。
7. 发布成功后才清除：

   ```text
   content_fts_dirty=0
   full_batch_incomplete=0
   ```

8. 最后把符合条件的 root 更新为 `ready` 并提交事务。

任何阶段失败或取消都必须回滚。返回结果不能只包含 `blocking_files`，还要包含 `ready` 和 `not_ready_reasons`。例如零阻断但仍有残留任务时，界面应显示“排除成功，正在修复残留任务”，不能显示“仍有 0 个阻断项”。

## 五、强力完成逻辑

### 5.1 按钮状态

“强力完成本次索引”保持常显，并满足以下条件时启用：

```text
ready=false
没有正在运行的索引、排除或恢复事务
not_ready_reasons 中至少有一个可修复原因
```

不能继续只用 `len(failed_files) > 0` 决定按钮状态。

### 5.2 两种执行路径

存在可见阻断文件时：

1. 对阻断文件执行最后一次重试。
2. 重试后再次列出失败文件。
3. 用户确认后人工排除剩余文件。
4. 收敛任务状态并发布 FTS。

阻断文件为 0、但索引仍未就绪时：

1. 不得因为 `failed_files=[]` 直接返回。
2. 显示真实未就绪原因。
3. 清理已排除文件的活动任务。
4. 将成功文件对应的残留旧任务改为 `invalidated`。
5. 对仍可见且未完成的文件生成可处理失败项；不得静默跳过。
6. 重建 `content_fts` 和 `files_fts`，或重新发布已有候选版本。
7. 执行 `integrity_check`、`foreign_key_check` 和最小搜索校验。
8. 全部通过后清除脏标记、结束未完成批次并将 root 标记为 `ready`。

如果存在真实活动索引任务，按钮禁用并提示等待；如果发现数据库完整性错误，禁止强行标记 `ready`，应显示诊断并保留原索引。

## 六、ZIP 错误状态收敛

ZIP 规划失败后可以回退到容器解析，但最终必须同时结束文件状态和任务状态。

状态规则：

| 错误 | 文件状态 | 任务状态 | 用户处理 |
|---|---|---|---|
| `ZIP_SIZE_LIMIT` | `skipped` | `complete` | 可查看原因、重试或人工排除 |
| `ZIP_FILE_COUNT_LIMIT` | `skipped` | `complete` | 可查看原因、重试或人工排除 |
| `BadZipFile` | `failed` 或 `failed_retryable` | `failed` | 修复文件、重试或人工排除 |
| 密码或不安全成员 | 现有明确的部分/跳过状态 | `complete` 或 `failed` | 按明确原因处理 |

安全限制不是程序崩溃，不应统一伪装成 `failed`；但任务必须进入终态，文件必须出现在“未成功索引”中，且排除后不能继续触发 `unfinished_tasks` 门禁。

## 七、界面修复

1. 顶部状态、搜索页、索引管理页和未成功索引页全部使用统一诊断结果。
2. 删除索引管理页无任务时写死的“索引已就绪”。
3. 未就绪时显示具体原因，例如：

   ```text
   文件已全部处理，但仍有 12 个残留任务和 1 个未发布全文索引
   ```

4. “强力完成”工具提示显示将要修复的状态，而不是在零阻断时显示“无需强力完成”。
5. 自动收敛失败时保留按钮可用，并提供“导出诊断”。

## 八、当前搜索算法

当前搜索不是纯粹的 SQLite FTS 倒排检索，实际流程如下：

1. 搜索前调用 `index_readiness()`；`ready=false` 时直接拒绝搜索。
2. 对查询做 NFKC、大小写、空格和连字符归一化。
3. 默认同时搜索文件名、路径、正文和 OCR。
4. 文件名和路径直接查询 `files` 表，条件为：

   ```sql
   LOWER(filename) LIKE '%关键词%'
   LOWER(path) LIKE '%关键词%'
   ```

   当前没有使用已经建立的 `files_fts MATCH`。

5. 正文虽然从 `content_fts` 读取，但普通条件仍是：

   ```sql
   content_fts.normalized_text LIKE '%关键词%'
   ```

   没有使用 FTS5 的 `MATCH` 倒排索引。

6. SQL 返回候选行后，Python 再逐行归一化、确认包含关系、生成上下文、计算命中次数、按文件合并、排序，最后才截取当前页和 `max_results`。
7. 正则、忽略空格或忽略连字符时，SQL 连 `LIKE` 条件也不加，直接读取全部候选行后在 Python 中匹配。

当前代码还会对包含中文的普通查询自动设置 `ignore_spaces=true`。因此同事搜索一个中文名称时，会进入全文全扫描路径：不仅扫描约 7,470 个文件名，还可能扫描现场约 225,000 个正文块，执行关联、重复合并和 Python 二次确认。

搜索运行在后台线程，所以窗口通常仍可响应；但搜索没有硬超时，取消只设置 token。SQLite 正在执行长 SQL 时不会及时检查 token，`SearchWorker.cancel()` 也没有调用 `sqlite3_interrupt()`。这解释了“等待十几分钟仍没有结果或取消不及时”。

## 九、搜索性能修复

### 9.1 P0 快速路径

1. 文件名/路径搜索改用 `files_fts MATCH`；trigram 不支持的短关键词才回退到小规模 `files` 表 `LIKE`。
2. 普通正文“完全包含、短语、全部关键词、任意关键词”先用 `content_fts MATCH` 取得候选，再保留 Python 二次确认，保证现有包含语义和上下文准确性。
3. 中文查询不得仅因为包含中文就自动退化为全库扫描。无空格的普通中文名称应直接走 FTS 候选路径。
4. 默认搜索若文件名先获得高置信命中，应立即展示文件名结果；正文结果可以随后补充，不能等全部正文扫描结束后才显示第一页。
5. 将文件名、路径和正文候选合并后尽早限制工作量；分页不能在全库扫描、全量排序之后才生效。
6. `SearchWorker` 保存当前 `DatabaseManager`，取消时同时调用 token 和 `interrupt_active_connections()`；SQLite 使用 progress handler 检查取消。
7. 增加搜索阶段、SQL 耗时、候选行数、确认结果数和总耗时日志；超过 2 秒记录慢查询原因。

### 9.2 兼容路径

- 正则表达式允许扫描，但必须显示“正则搜索可能较慢”、持续进度并支持及时取消。
- 真正需要跨空格、跨连字符匹配时，后续增加专用 compact-normalized FTS 字段或索引，不能长期依赖全正文 Python 扫描。
- 对少于 trigram 最小长度的关键词使用受控回退，并限制范围、候选量和超时。

### 9.3 80GB 资料场景的性能目标

索引完成后的搜索只查询本地数据库和 FTS 索引，不应重新读取约 80GB 的源文件。搜索耗时主要取决于 FTS 索引规模、关键词命中数量、磁盘性能和结果整理量。

以现场当前普通名称搜索超过 10 分钟（按 600 秒计算）为对照，第一阶段目标如下：

| 搜索场景 | 当前表现 | 优化后目标 | 预计时间减少 |
|---|---:|---:|---:|
| 普通中文文件名（至少 3 个字符） | 600 秒以上 | 0.2～1 秒 | 约 99.8%～99.98% |
| 默认文件名、路径和正文搜索 | 600 秒以上 | 1～3 秒 | 约 99.5%～99.9% |
| 高频正文词、命中数量很大 | 600 秒以上 | 3～15 秒 | 约 97.5%～99.7% |
| 正则表达式或 1～2 字超短词 | 可能超过 600 秒 | 取决于扫描范围 | 不承诺固定比例 |

换算示例：

```text
600 秒优化到 1 秒：时间减少 99.83%，约快 600 倍
600 秒优化到 3 秒：时间减少 99.50%，约快 200 倍
```

以上是基于现场数据规模、当前超过 10 分钟的表现和算法复杂度得出的验收目标，不是已完成的实测结论。实现后必须使用同事当时的实际搜索名称，在同一数据库、同一电脑和相同过滤条件下进行修改前后对照，并记录最终百分比。

## 十、搜索进度与取消需求

### 10.1 总体原则

搜索必须显示可理解的处理状态，但不得为无法观测的 SQLite 单条 SQL 伪造精确百分比。

- 普通 FTS 快速搜索显示阶段、已找到文件数和耗时，使用忙碌进度条。
- 正则、超短词和其他扫描型搜索显示真实的已检查数量、总数量和百分比。
- 搜索在 2 秒内完成时保持简洁，避免进度区域闪烁；超过 2 秒后展开详细进度。
- 文件名高置信结果先展示，正文结果可以继续追加，不能等待全部正文处理完成后才显示第一页。

### 10.2 普通 FTS 搜索进度

普通文件名、路径和正文搜索至少按以下阶段发送进度事件：

```text
正在搜索文件名和路径...
正在搜索正文...
正在确认匹配结果... · 候选 126 条 · 已找到 26 个文件
正在排序和整理结果...
搜索完成 · 找到 26 个文件 · 用时 0.8 秒
```

SQLite FTS `MATCH` 单条查询没有可靠的内部完成比例，因此这些阶段使用忙碌进度条，不能显示“63%”之类的推测值。

进度事件至少包含：

```text
stage
phase_label
elapsed_ms
metadata_candidates
content_candidates
checked_candidates
confirmed_files
progress_kind = busy
can_cancel
```

### 10.3 扫描型搜索进度

正则、超短词或其他必须扫描候选集合的查询，在查询开始前确定本次需要检查的候选总数，并按批次更新：

```text
正在扫描正文 38,420 / 225,032
已检查 17%
已找到 84 个文件
已用时 6 秒
```

进度事件至少包含：

```text
stage = scanning
phase_label
checked_candidates
total_candidates
confirmed_files
percent
elapsed_ms
progress_kind = determinate
can_cancel
slow_reason
```

只有 `total_candidates` 在本次搜索开始前已确定时才显示百分比。无法可靠确定总量时降级为忙碌进度，不显示 ETA 或虚假百分比。

### 10.4 慢搜索提示

搜索超过 2 秒后显示当前慢因，例如：

```text
正在执行正则全文扫描
关键词过短，正在使用兼容搜索
命中范围较大，正在整理结果
数据库正在等待其他索引事务
```

超过约定慢查询阈值时记录搜索词摘要或哈希、查询模式、搜索范围、阶段耗时、候选数量、确认文件数和取消结果。日志不得记录命中文件正文。

### 10.5 取消行为

1. 搜索开始后显示“停止搜索”控件。
2. 点击停止后立即设置 `CancelToken`。
3. `SearchWorker` 同时调用当前 `DatabaseManager.interrupt_active_connections()`，中断正在执行的 SQLite 语句。
4. SQLite 连接设置 progress handler，定期检查取消状态。
5. 目标是在点击停止后 1 秒内退出搜索、清理线程和数据库连接。
6. 取消后保留已经展示的结果，并明确显示“搜索已停止，当前显示部分结果”。
7. 新搜索到来时先取消旧搜索，不允许旧结果覆盖新查询结果。

## 十一、验证要求

### 11.1 索引开放

1. 构造“阻断项 0、残留 failed/superseded 任务、FTS dirty、root incomplete”的数据库。
2. 验证人工排除最后一个阻断项后自动开放搜索。
3. 验证零阻断时“强力完成”可用且能修复原数据库。
4. 验证可见失败文件仍阻止发布，已排除失败任务不再阻止发布。
5. 验证 ZIP 安全限制和损坏 ZIP 的文件/任务状态一致。
6. 验证事务失败时原索引、审计和 FTS 全部回滚。
7. 验证 `integrity_check=ok`、`foreign_key_check` 无错误。

### 11.2 搜索性能、进度和准确性

在接近现场规模（约 7,500 个文件、225,000 个正文块）的数据库上验证：

1. 普通中文文件名称搜索走 `files_fts MATCH`，不读取全部正文块。
2. 普通正文关键词搜索走 `content_fts MATCH`，结果与修改前逐行确认结果一致。
3. 文件名结果可以先显示；首次可见结果目标不超过 1 秒。
4. 常见正文查询目标不超过 3 秒；高频词应有明确候选上限和提示。
5. 搜索取消目标不超过 1 秒，不留下搜索线程或 SQLite 活动语句。
6. exact、phrase、all、any、OCR、中文、英文、文件名、路径和正文分别做准确性回归。
7. 普通 FTS 搜索只显示阶段忙碌进度，不显示伪造百分比。
8. 扫描型搜索显示真实的 `checked/total` 和百分比，且数值单调递增。
9. 验证文件名结果可以先展示，后续正文结果追加时不重复、不丢失且排序稳定。
10. 搜索超过 2 秒后显示慢因，2 秒内完成的搜索不产生明显进度闪烁。
11. 使用现场实际搜索名称，在同一数据库和电脑上记录修改前后耗时及时间减少百分比。

以上时间是同规模本地 SSD 环境的验收目标，不作为所有电脑和所有正则表达式的固定承诺。

## 十二、实施顺序

1. 统一任务终态和 `readiness_diagnostics()`。
2. 修复人工排除后的自动状态收敛。
3. 修复零阻断场景下的“强力完成”入口和核心事务。
4. 修复 ZIP 文件状态与任务状态一致性。
5. 修正各页面状态文案并验证原数据库升级使用。
6. 将文件名和普通正文查询切换到 FTS `MATCH` 快速路径。
7. 增加阶段进度、扫描型真实百分比、结果流式展示和及时取消。
8. 增加慢查询日志和同规模性能/准确性验证。

本方案不要求重新解析已经成功索引的源文件，也不要求删除或重建整个数据库。

## 十三、实施与快速验证结果（2026-08-03，1.3.7）

本次已按上述顺序完成实现：

1. `index_readiness()` 统一检查文件阻断、活动解析任务、活动批次、root 状态、`content_fts_dirty`、`full_batch_incomplete` 和未发布候选，并向三个页面提供同一份原因列表。
2. 人工排除最后一个阻断项后，在同一事务内收敛残留任务、清理并重建 FTS、刷新 root/批次状态；任一步失败或取消均回滚。
3. “强力完成本次索引”保持常显；有阻断项或零阻断但有可修复残留状态时启用。零阻断修复在后台线程直接操作原数据库，不创建数据库副本。
4. ZIP 安全限制保持文件 `skipped` 并显示在未成功列表，对应解析任务进入 `complete`；损坏 ZIP 仍为失败终态。
5. 普通中文/英文名称、路径和正文查询优先使用 trigram FTS5 `MATCH`，再做字符串确认；无空格的普通中文查询不再自动退化为全库扫描。
6. 文件名/路径结果先显示，正文结果随后合并；普通 FTS 显示阶段忙碌进度，扫描型搜索显示真实候选进度；取消同时触发 token、SQLite progress handler 和 `sqlite3_interrupt()`，已有部分结果继续保留。
7. 索引管理页和顶部状态不再把“没有任务运行”误写为“索引已就绪”；零阻断残留状态会明确显示待修复原因。

快速验证结果：

```text
聚焦功能回归：67 passed
数据库、分发和归档回归：22 passed
最后搜索/UI 补丁回归：25 passed

源码 --self-test：SELF_TEST_OK
源码 --validate-manual-exclusion：passed=true，integrity_check=ok，foreign_key_errors=0
源码 --validate-ui：UI_VALIDATION_OK，1280x800

冻结 EXE --self-test：exit_code=0
冻结 EXE --validate-manual-exclusion：exit_code=0
冻结 EXE --validate-ui：exit_code=0

DISTRIBUTION_VERIFICATION_OK files=6865 bytes=823559809
RELEASE_ARCHIVE_OK files=6866 source_bytes=824419291
ZIP_SHA256=DB8580B89DD2A1CFEA21110443D4BFD2A839B8C60C93CBA73CCAA9F9C1620D1C
```

按本次要求未运行原始数据集、80GB 现场库或大规模搜索性能对照，因此本次证据关闭功能逻辑、事务回滚、轻量准确性、控件状态、冻结启动和分发完整性，不声明已经完成 80GB 性能百分比验收。现场名称搜索的实际降幅仍应在同一数据库、同一电脑上记录修改前后耗时后填写。
