# 数据库结构说明

数据库默认位置：

```text
%LOCALAPPDATA%\LocalFullTextSearch\data\search_index.db
```

核心表：

- `roots`：用户配置的搜索根目录。
- `files`：文件元数据、解析状态、错误信息和增量指纹。
- `content_blocks`：按页、段落、行、幻灯片、OCR 等可定位单位保存的内容块。
- `content_fts`：SQLite FTS5 虚表，使用 trigram tokenizer 优先支持强包含召回。
- `short_tokens`：1 到 2 字符短词辅助表。
- `parse_tasks`：解析任务状态预留表，用于断点续建和后续队列化。
- `search_history`：搜索历史预留表。

写入策略：

- 每个文件成功解析后立即事务提交。
- 每个线程独立创建 SQLite 连接。
- 数据库启用 WAL 模式。
- 文件删除时会移除对应 FTS 记录并把文件标记为 deleted。
